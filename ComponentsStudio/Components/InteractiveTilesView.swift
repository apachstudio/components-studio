// Interactive Tiles was removed from Components Studio.
//
// The component, its specs, presets and catalog entry were deleted. The
// file is intentionally left empty (rather than removed from disk) so the
// Xcode project file doesn't need editing while the IDE is open. The
// matching Metal shaders (`interactiveTilesLiquidMetal`,
// `interactiveTilesHolographic`) remain in StudioShaders.metal as unused,
// harmless dead code.
